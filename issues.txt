issues that i hv gotten,
1. at signup, the data is not being saved to the database. i think this is due to data missmatch the frontend and backend. Please check the data models and ensure that the fields match correctly between the frontend form and the backend API. Also, verify that the API endpoint is correctly receiving the data and that there are no validation errors preventing the data from being saved.
2. The login functionality is not working as expected. Users are unable to log in even with correct credentials. Please check the authentication logic in the backend and ensure that the password hashing and verification process is implemented correctly. Also, verify that the session management is functioning properly.
3. There are issues with the user profile update feature. Changes made to the user profile are not being reflected in the database. Please check the update logic in the backend and ensure that the correct fields are being updated. Additionally, verify that the database update operation is successful and that there are no errors being thrown during the process. Make sure to handle any potential exceptions and provide appropriate feedback to the user in case of failure.
4. the input fields in the frontend form are not properly validated. Please ensure that all required fields have appropriate validation rules in place, such as checking for empty fields, valid email formats, and password strength. Implement client-side validation to provide immediate feedback to users before submitting the form to the backend. Additionally, make sure that the backend also performs server-side validation to prevent any invalid data from being saved to the database.
5. There are performance issues with the application, especially when handling a large number of concurrent users. Please review the backend code for any potential bottlenecks and optimize database queries to improve response times. Consider implementing caching strategies where appropriate to reduce the load on the database and improve overall performance.
6. the other issue is that the database is constantly rejecting the registration numbers being submitted. Please check the database schema and ensure that the registration number field is correctly defined and that there are no constraints or unique key violations causing the rejections. Additionally, verify that the data being sent from the frontend matches the expected format for the registration number and that any necessary transformations or validations are being applied before saving to the database. so there is a hard coded format that was misinterpreted , the right format should be xx/x/xxx/{and alpgabet}/{any number} and the backend should be updated to accept this format.
7. In the same issue of field missmatch between frontend and backend it is the course work creation feature. The data being sent from the frontend does not match the expected format in the backend, leading to errors when trying to create new coursework entries. Please review the data models for both the frontend and backend and ensure that they are aligned. Update the backend API to accept the correct data structure and implement any necessary validation to ensure that the coursework data is being processed correctly. Additionally, provide clear error messages to users if their input does not meet the required format or criteria.   this is not only occurring in the coursework creation feature but also in other features.
8. So at the login page i am something with text "live group formation" that is not supposed to be there. Please check the frontend code for any unintended elements or text that may have been added to the login page. Remove any unnecessary content and ensure that the login page only contains the relevant fields and information for user authentication. Additionally, verify that the styling and layout of the login page are consistent with the overall design of the application.
9. the ai assistant feature is not functioning correctly. There is a key already in the websites variables but the feature is not responding as expected. Please check the integration of the AI assistant with the backend and ensure that the API key is being used correctly. Verify that the requests to the AI service are being sent properly and that the responses are being handled appropriately. Additionally, review any error logs or messages to identify potential issues with the AI assistant's functionality and implement necessary fixes to ensure it operates as intended. 
10. The front end has default values in the input fields that are not being cleared when the user starts typing. Please check the frontend code to ensure that the input fields are properly initialized and that any default values are cleared when the user interacts with the form. Implement event listeners to handle user input and update the field values accordingly. Additionally, verify that the form submission process correctly captures the user's input without retaining any default values.
11. i want to add a new faculty but i dont know if the site will be able to seperate the faculty data from the existing faculties. Please check the backend logic for handling faculty data and ensure that it can accommodate new entries without conflicts. Verify that the database schema allows for multiple faculties and that there are no unique constraints that would prevent adding a new faculty. Additionally, implement proper validation and error handling to ensure that the new faculty data is saved correctly and does not interfere with existing records.
12. With issues concerning faculty data, there are some course units and courses that exist on only one faculty and some that exist on multiple faculties. Please review the database schema and ensure that the relationships between faculties, courses, and course units are properly defined. Implement logic in the backend to handle cases where a course or course unit may belong to multiple faculties, and ensure that the data is being saved and retrieved correctly. Additionally, provide clear feedback to users when they are adding or updating course data to avoid confusion regarding faculty associations. Apart from course units, courses are also found on only one fuculty not multiple faculties. Please ensure that the backend logic correctly handles these scenarios and that the frontend displays the appropriate information to users based on their faculty selection. so currently that data thats one on the data base, belongs to that faculty  present and only one of the course units is shared across multiple faculties. Please ensure that the backend logic correctly handles these scenarios and that the frontend displays the appropriate information to users based on their faculty selection. Additionally, implement any necessary validation to prevent users from associating a course or course unit with multiple faculties if it is not intended to be shared.
13. Data entry at the login page, all fields must be entered before the user can proceed. Please implement validation logic on the frontend to ensure that all required fields are filled out before allowing the user to submit the login form. Additionally, provide clear error messages or visual cues to indicate which fields are missing or incomplete. On the backend, ensure that server-side validation is also in place to prevent any incomplete data from being processed.
14. so we r adding a new faculty and each faculty has its own courses and course units and coordinator/s. So a student at another faculty should not be able to see the courses and course units and of a different faculty unles its a shared course unit. Please review the access control logic in the backend to ensure that students can only view and interact with the courses and course units associated with their own faculty. Implement proper authorization checks to restrict access based on the user's faculty affiliation. Additionally, update the frontend to display only the relevant courses and course units for the logged-in student's faculty, ensuring a clear separation of data between faculties.
16. I want to add a new heirachy, which is admin, and this admin will have access to all faculties and their courses and course units. Please review the backend logic for user roles and permissions to ensure that the new admin role is properly defined and has the necessary access rights. Implement authorization checks to allow the admin to view and manage data across all faculties, while ensuring that regular users are restricted to their own faculty's data. Additionally, update the frontend to provide appropriate interfaces and options for the admin role, allowing them to perform administrative tasks effectively. so the admin page should have a dashboard that shows all faculties and their respective courses and course units including students data, with options to manage and update data as needed. Ensure that the admin role is clearly distinguished from other user roles in the system, and provide appropriate feedback and error handling for any actions taken by the admin. so a student can be a coordinator in a faculty and an admin. this admin level 
should be able to view all faculties and their courses and course units, while a student coordinator should only have access to their own faculty's data. Please ensure that the backend logic correctly enforces these access controls based on the user's role and faculty affiliation. Additionally, update the frontend to provide clear navigation and options for both admin and student coordinator roles, allowing them to perform their respective tasks without confusion or overlap. Note this profile jus like the coordinator status it should be set directly in the backend and not on the frontend. so the admin should be able to view all faculties and their courses and course units, while a student coordinator should only have access to their own faculty's data. Please ensure that the backend logic correctly enforces these access controls based on the user's role and faculty affiliation. Additionally, update the frontend to provide clear navigation and options for both admin and student coordinator roles, allowing them to perform their respective tasks without confusion or overlap. Note this profile just like the coordinator status it should be set directly in the backend and not on the frontend.